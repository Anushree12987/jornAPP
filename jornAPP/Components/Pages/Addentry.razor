@page "/addentry"
@using jornAPP.Components.Models
@using jornAPP.Services
@inject JournalService JournalService
@inject NavigationManager Nav

<h1>Add Journal Entry</h1>

<div class="form-container">

    <!-- Title -->
    <label for="title">Title</label>
    <input id="title" placeholder="Enter title..." @bind="entry.Title" class="form-control mb-3" />

    <!-- Content -->
    <label for="content">Your Thoughts</label>
    <textarea id="content" placeholder="Write your entry here..." @bind="entry.Content" class="form-control mb-3" rows="10"></textarea>

    <!-- Primary Mood -->
    <label>Primary Mood</label>
    <select @bind="entry.PrimaryMood" class="form-select mb-3">
        <option value="">Select Primary Mood</option>
        <option>😊 Happy</option>
        <option>😌 Calm</option>
        <option>😐 Okay</option>
        <option>😔 Sad</option>
        <option>😤 Stressed</option>
    </select>

    <!-- Secondary Moods -->
    <label>Secondary Mood 1</label>
    <select @bind="entry.SecondaryMood1" class="form-select mb-3">
        <option value="">Select Secondary Mood 1</option>
        <option>😊 Happy</option>
        <option>😌 Calm</option>
        <option>😐 Okay</option>
        <option>😔 Sad</option>
        <option>😤 Stressed</option>
    </select>

    <label>Secondary Mood 2</label>
    <select @bind="entry.SecondaryMood2" class="form-select mb-3">
        <option value="">Select Secondary Mood 2</option>
        <option>😊 Happy</option>
        <option>😌 Calm</option>
        <option>😐 Okay</option>
        <option>😔 Sad</option>
        <option>😤 Stressed</option>
    </select>

    <!-- Category -->
    <label>Category</label>
    <select @bind="entry.Category" class="form-select mb-3">
        <option value="">Select Category</option>
        <option>Positive</option>
        <option>Neutral</option>
        <option>Negative</option>
    </select>

    <!-- Tags -->
    <label>Tags</label>
    <input placeholder="Add tags, comma-separated" @bind="entry.Tags" class="form-control mb-3" />

    <!-- Buttons -->
    <div class="button-group">
        <button class="btn btn-primary" @onclick="SaveEntry">Save</button>
        <button class="btn btn-secondary" @onclick="Cancel">Cancel</button>
    </div>
</div>

<style>
body {
    background: #121212;
    color: #fff;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
}

h1 {
    text-align: center;
    font-size: 2.5rem;
    color: #ffd700;
    margin: 30px 0 20px 0;
    text-shadow: 1px 1px 4px rgba(0,0,0,0.6);
}

.form-container {
    max-width: 700px;
    margin: 0 auto 50px auto;
    padding: 25px 30px;
    background: #1e1e1e;
    border-radius: 16px;
    box-shadow: 0 12px 25px rgba(0,0,0,0.5);
}

label {
    display: block;
    font-weight: 600;
    margin-bottom: 6px;
    color: #ffd700;
}

.form-control, .form-select {
    width: 100%;
    background: #2a2a2a;
    color: #ffffff;
    border: 1px solid #444;
    border-radius: 12px;
    padding: 12px 14px;
    font-size: 1rem;
    transition: all 0.2s ease-in-out;
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.4);
}

.form-control::placeholder, .form-select option {
    color: #ccc;
}

.form-control:focus, .form-select:focus {
    outline: none;
    border-color: #ffd700;
    box-shadow: 0 0 8px #ffd700;
}

textarea.form-control {
    min-height: 160px;
    resize: vertical;
}

.button-group {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 25px;
}

button.btn-primary {
    background: #ffd700;
    color: #121212;
    border: none;
    padding: 12px 28px;
    border-radius: 14px;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s ease-in-out;
    box-shadow: 0 4px 10px rgba(0,0,0,0.4);
}

button.btn-primary:hover {
    opacity: 0.95;
    transform: translateY(-2px);
}

button.btn-secondary {
    background: #2a2a2a;
    color: #ffffff;
    border: 2px solid #ffd700;
    padding: 12px 28px;
    border-radius: 14px;
    cursor: pointer;
    font-weight: 600;
    transition: all 0.2s ease-in-out;
}

button.btn-secondary:hover {
    background: #ffd700;
    color: #121212;
}
</style>

@code {
    JournalEntry entry = new JournalEntry
    {
        EntryDate = DateTime.Today,
        CreatedAt = DateTime.Now,
        UpdatedAt = DateTime.Now
    };

    async Task SaveEntry()
    {
        var existing = await JournalService.GetEntryByDateAsync(entry.EntryDate);

        if (existing != null)
        {
            existing.Title = entry.Title;
            existing.Content = entry.Content;
            existing.PrimaryMood = entry.PrimaryMood;
            existing.SecondaryMood1 = entry.SecondaryMood1;
            existing.SecondaryMood2 = entry.SecondaryMood2;
            existing.Category = entry.Category;
            existing.Tags = entry.Tags;
            existing.UpdatedAt = DateTime.Now;

            await JournalService.UpdateEntryAsync(existing);
        }
        else
        {
            entry.CreatedAt = DateTime.Now;
            entry.UpdatedAt = DateTime.Now;

            await JournalService.AddEntryAsync(entry);
        }

        Nav.NavigateTo("/entries");
    }

    void Cancel() => Nav.NavigateTo("/entries");
}
